"""Hand-written game graphs (root namespace).

Each module here builds and returns a `GameGraph` via the
`core.navigation.builder` DSL. The demo wiring lives in `_demo.py`; real
production graphs (per game / per launcher) will sit alongside it.
"""
